# express-sql
Express SQL app for collecting sales leads.


  change directory:
    > cd express-sql

  install dependencies:
    > npm install

  run the app:
    > SET DEBUG=express-sql:* & npm start